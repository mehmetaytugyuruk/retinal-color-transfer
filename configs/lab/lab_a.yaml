representation:
  name: lab_a
  approved_for_cache_build: true
  params: {}
  tensor_scaling:
    stored_dtype: uint8
    stored_range: [0, 255]
    canonical_scaling: uint8_divide_by_255
    canonical_range: [0.0, 1.0]
    channel_names: [a_encoded, a_encoded, a_encoded]
    cache_extension: .png
    is_binary: false
    repeated_from_single_channel: true
  channel_order: repeated_OpenCV_a_encoded
  numeric_range: [0, 255]
  notes: OpenCV LAB encoded a channel repeated to three channels for chroma-channel ablation.
