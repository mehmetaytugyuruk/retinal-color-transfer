representation:
  name: lab
  approved_for_cache_build: true
  params: {}
  tensor_scaling:
    stored_dtype: uint8
    stored_range: [0, 255]
    canonical_scaling: uint8_divide_by_255
    canonical_range: [0.0, 1.0]
    channel_names: [L_encoded, a_encoded, b_encoded]
    cache_extension: .png
    is_binary: false
    repeated_from_single_channel: false
  channel_order: OpenCV_L_a_b_encoded
  numeric_range: [0, 255]
  notes: OpenCV COLOR_RGB2LAB 8-bit encoding; a/b are encoded uint8 channels, not physical CIELAB values.
