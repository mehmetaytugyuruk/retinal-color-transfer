representation:
  name: hsv_v
  approved_for_cache_build: true
  params: {}
  tensor_scaling:
    stored_dtype: uint8
    stored_range: [0, 255]
    canonical_scaling: uint8_divide_by_255
    canonical_range: [0.0, 1.0]
    channel_names: [V, V, V]
    cache_extension: .png
    is_binary: false
    repeated_from_single_channel: true
  channel_order: repeated_OpenCV_V
  numeric_range: [0, 255]
  notes: OpenCV HSV V channel repeated to three channels for single-channel ablation.
